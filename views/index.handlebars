<h1>BURGERS!</h1>

<h2>Burger Ideas to be tried!</h2>

<ul>
  {{#each burgs}}
  {{#unless devoured}}
  {{> burgs/eatMe devoured=true}}
  {{/unless}}
  {{/each}}
</ul> 



<h2>Burgers tested out</h2>

<ul>
  {{#each burgs}}
  {{#if devoured}}
  {{> burgs/eatMe devoured=false}}
  {{/if}}
  {{/each}}
</ul>

<h2>New burger idea</h2>
<form class="create-form">

  <div class="form-group">
    <label for="bg">Burg Name:</label>
    <input type="text" id="bg" name="name">
  </div>


  <button type="submit">Add Burg</button>
</form>